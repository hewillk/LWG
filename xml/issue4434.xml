<?xml version='1.0' encoding='utf-8' standalone='no'?>
<!DOCTYPE issue SYSTEM "lwg-issue.dtd">

<issue num="4434" status="New">
<title>`meta::is_accessible` does not need to consider incomplete <i>D</i></title>
<section><sref ref="[meta.reflection.access.queries]"/></section>
<submitter>Jakub Jelinek</submitter>
<date>27 Oct 2025</date>
<priority>99</priority>

<discussion>
<p>
<sref ref="[meta.reflection.access.queries]"/> says that
`is_accessible(r, ctx)` throws if:
<blockquote>
`r` represents a direct base class relationship (<i>D</i>,<i>B</i>)
for which <i>D</i> is incomplete.
</blockquote>
However, the only way to get access to a direct base relationship is
through `bases_of`/`subobjects_of` and those throw if the class is incomplete,
so I don't see how an `is_base` reflection could have ever incomplete <i>D</i>.
</p>
</discussion>

<resolution>
<p>
This wording is relative to <paper num="N5014"/>.
</p>

<ol>

<li><p>Modify <sref ref="[meta.reflection.access.queries]"/> as indicated:</p>

<blockquote>
<p>-4- <i>Throws</i>: `meta::exception` if<del>:</del>
<ul>
<li><del>(4.1) &mdash;</del>
`r` represents a class member for which <code><i>PARENT-CLS</i>(r)</code>
is an incomplete class <del>or</del><ins>.</ins>
</li>
<li><del>(4.2) &mdash;
`r` represents a direct base class relationship (<i>D</i>,<i>B</i>)
for which <i>D</i> is incomplete.</del>
</li>
</ul>
</p>
</blockquote>
</li>
</ol>
</resolution>

</issue>
